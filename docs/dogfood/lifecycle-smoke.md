# PullOps Lifecycle Smoke Test

This note was created by the dogfood lifecycle smoke issue to verify the happy PullOps issue-to-draft-PR lifecycle.

It exercises the operation flow across `pullops:issue:implement`, `pullops:pr:review`, and `pullops:pr:finalize` without changing runtime code.
