# Keep issue implementation focused

The `pullops-issue-implement` skill should implement the linked issue as written, while allowing Adjacent Work needed to complete it correctly. Unrelated defects, broad refactors, or larger design problems discovered during implementation should be recorded as follow-up candidates rather than silently folded into the issue PR.
