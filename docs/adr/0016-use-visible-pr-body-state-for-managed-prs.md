# Use visible PR body state for managed PRs

PullOps identifies and tracks PullOps-Managed PRs through a visible PR State Marker in the PR body, with the configured Branch Prefix as a secondary guardrail. The visible marker is the durable source of truth for status and cycle counts, while the branch name helps humans and workflows recognize PullOps-created branches.
