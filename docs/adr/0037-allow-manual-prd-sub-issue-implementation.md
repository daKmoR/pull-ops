# Allow manual PRD sub-issue implementation

PullOps allows a maintainer to label a native PRD sub-issue with `pullops:implement` directly, even when the parent PRD Issue is not labeled. Parent PRD implementation remains the coordinated automation path, but direct sub-issue labeling is the manual selection path for choosing a specific sub-issue and order without blocking on parent-level automation.
