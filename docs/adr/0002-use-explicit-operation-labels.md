# Use explicit operation labels

PullOps uses explicit Operation Labels for each requested operation and separate Status Labels for progress state. The issue implementation label stays `pullops:implement` for both Leaf Issues and PRD Issues; PRD behavior is inferred from native GitHub sub-issues so humans do not need to choose a separate PRD-specific command label.
