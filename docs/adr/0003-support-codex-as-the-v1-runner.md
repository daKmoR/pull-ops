# Support Codex as the v1 runner

PullOps v1 supports Codex as the default and tested AI runner, with `codex-cli` as the default Runner Adapter and a configurable Runner Command in the Target Repository. This avoids a premature multi-provider abstraction while still naming the execution adapter explicitly when GitHub Actions uses `codex-action`.
